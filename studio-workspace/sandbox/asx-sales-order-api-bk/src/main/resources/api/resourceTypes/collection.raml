#%RAML 1.0 ResourceType
    description: Collection of available <<resourcePathName>> 
    get:
      description: Collection of available <<resourcePathName>> 
      responses:
        200:
          body:
            application/json:

    post:
      description: |
        Add new <<resourcePathName>> for furthur processing.
      body:
        application/json:
      responses:
        200:
          body:
            application/json:
              example: |
                { "message": "The<<resourcePathName>> has been properly entered" }  collection-item:
